# Environment configuration
# Set to 'development' for local development, 'production' for server deployment
REACT_APP_ENV=development

# Base path for routing (leave empty for root path in development)
REACT_APP_BASE_PATH=

# Library path (used for navigation from homepage)
REACT_APP_LIBRARY_PATH=/my-library