# Production environment configuration
REACT_APP_ENV=production

# Base path for routing (set to /my-library for server deployment)
REACT_APP_BASE_PATH=/my-library

# Library path (used for navigation from homepage)
REACT_APP_LIBRARY_PATH=/my-library