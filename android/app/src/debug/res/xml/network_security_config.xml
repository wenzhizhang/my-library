<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only; this file replaces the release policy, it never relaxes it in release.
     A developer running the backend on their own machine (./gradlew assembleDebug
     -PbaseUrl=http://127.0.0.1:8000) reaches it over plain HTTP, which release forbids outright.
     Only loopback is exempted: 127.0.0.1 via `adb reverse`, 10.0.2.2 as the emulator's host alias. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
