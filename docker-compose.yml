services:
  backend:
    image: ${BACKEND_IMAGE:-my-library-backend:local}
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        SERVER_IP: ${SERVER_IP:-localhost}
    ports:
      - '8000:8000'
    environment:
      - PYTHONUNBUFFERED=1
      - SERVER_IP=${SERVER_IP:-localhost}
    volumes:
      - ./demo.db:/app/demo.db
    restart: unless-stopped

  frontend:
    image: ${FRONTEND_IMAGE:-my-library-frontend:local}
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - '80:80'
    depends_on:
      - backend
    environment:
      - REACT_APP_ENV=production
      # 本地开发不设置 DOMAIN → 启动脚本使用 HTTP-only 模式
      - DOMAIN=${DOMAIN:-}
      - CERTBOT_EMAIL=${CERTBOT_EMAIL:-}
    restart: unless-stopped
