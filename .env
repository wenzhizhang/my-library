# Docker Compose Environment Variables
# Set your server public IP address here
SERVER_IP=your-server-public-ip

# Example:
# SERVER_IP=111.229.109.204
# SERVER_IP=dingfengbo.top

# Version is auto-managed via VERSION file (X.Y.Z format, e.g., 0.1.0)
# Patch version auto-increments on each './build.sh push'
# TAG is derived from VERSION (vX.Y.Z) — do not set manually
